server {
    listen 80;
    server_name texttextify.com www.texttextify.com;
    return 301 https://$host$request_uri;

    location / {
        proxy_pass http://front_container:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
  